# Android-flavored assistant-backend deps (ADR-012 step 1).
# Substitutions already applied:
#   uvicorn[standard] -> plain uvicorn (no uvloop/httptools/watchfiles)
#   firebase-admin    -> DROP
#   mcp               -> DROP (stdio cannot spawn on Android)
#
# Constraints otherwise match assistant-backend/pyproject.toml.
# This file is the honest "does Option A package the existing stack?" set.
# Expect Chaquopy pip to fail on pydantic-core (>=2), orjson, cryptography>=43,
# and openai's jiter dependency — see docs/spikes/chaquopy-wheels.md.
fastapi>=0.115
uvicorn>=0.32
aiosqlite>=0.20
openai>=1.50
cryptography>=43
google-auth>=2.57.0
google-auth-oauthlib>=1.2
requests>=2.34.2
aiohttp>=3.9
orjson>=3.12.0
pyyaml>=6.0.3
croniter>=1.0
